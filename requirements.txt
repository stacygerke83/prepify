# --- Core ---
flask>=2.3,<3.0
werkzeug>=2.3,<3.0
flask-cors>=4.0,<5.0
python-dotenv>=1.0,<2.0

# --- Data / DB (optional but recommended if pantry uses SQLAlchemy) ---
sqlalchemy>=2.0,<3.0

# --- HTTP / Utilities ---
requests>=2.32,<3.0

# --- Testing & Coverage ---
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# --- Linting / Formatting / Security ---
flake8>=7.0,<8.0
black>=24.10,<25.0
isort>=5.13,<6.0
bandit>=1.7,<2.0

# --- Type checking (optional) ---
mypy>=1.11,<2.0
types-requests>=2.32,<3.0

# --- WSGI server ---
gunicorn>=22.0,<23.0
